# 0.2.35

Added `log`, `logIf`, `logIfNot`, `info`, `infoIf`, `infoIfNot`, `debug`, `debugIf`,
`debugIfNot`, `warn`, `warnIf` and `warnIfNot`.
Implemented manipulators for members.
UNSTABLE: Implemented `register` to register enums, encoders and decoders.

# 0.2.15

Added `crashIf` and `crashIfNot` utility functions

# 0.2.14

Better utility types responses

# 0.2.12

Avoid util types to assign members
Check if a module or exposed type is extending a non-exposed type

# 0.2.9

Added `omitType` utility
Added `pickType` utility
Added `cleanType` utility

# 0.2.7

Added `/utils` exports!
Added `intersectionType` utility
Added `partialType` utility
Added `cleanPartialType` utility
Better `Constructor` definition


# 0.2.4

Export `expose` function

# 0.2.3

Rename `exposes` singleton to `enums`

# 0.2.2

Export `exposes` singleton

# 0.2.1

Rename `Structure` decorator to `Expose`
Create `expose` function to process enums and other kind of datatypes (in future...)
Improved types for decorators
Export types from `type.ts` as `/types`

# 0.1.27

Use received client arguments to construct local modules

# 0.1.17

Serializable modules may be added to structures

# 0.1.14

Export more interfaces

# 0.1.11

Export `SdkOptions`

# 0.1.10

Export `SdkOptions`

# 0.1.9

Replace project name and import the sdk core correctly

# 0.1.8

Change JSON data transfer with raw data transfer
Change `BigSerializer` module with `TinySerializer`
Implement `Packager` to decode and encode data
Changed license to `Restricted Use License`